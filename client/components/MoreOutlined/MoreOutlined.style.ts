import styled from 'styled-components';

export const Button = styled.button`
  position: absolute;
  background: none;
  border: none;
  top: 4px;
  right: 0;
  cursor: pointer;
`;

export const Image = styled.img`
  width: 22px;
  height: 22px;
`;
